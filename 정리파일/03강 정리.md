# 암호학 개요
## 암호학(Cryptology)의 정의
암호학(Cryptology)이란 : 암호기술(cryptography), 암호해독(cryptanalysis)
## 암호에서 사용하는 이름

앨리스, 밥, 이브, 맬로리, 트렌트, 빅터

앨리스와 밥 : 앨리스는 메시지를 전송하고 밥이 수신을 하는 모델에 사용

이브 : 소극적인 공격자

맬로리 : 악의를 가진(malicious) 공격자를 의미, 적극적공격

앨리스와 밥 : Ron Rivest가 [1979]년에 처음으로 사용

* GPT의견 : 시험에서는 “이브=도청, 맬로리=악성 공격” 정도만 알면 충분

## 송신자, 수신자, 도청자
도청지 이브(eavesdropper)는 사람만 의미하는것이 아님

도청용 기계, 프로그램 일 수 있다.
## 암호화와 복호화
암호(cryptography) -> 기밀성(confidentiality, 비밀성)을 유지한다.
## 암호화와 복호화의 기호적 표현 *3
$$
<u>C = E_k(P)<u>
$$
$$
<u>P = D_k(C)<u>
$$

[정통수학]에 기초를 둔다

[테스트 기간]을 거쳐야 한다

키 스페이스(Key space) : =키 도메인 무차별공격과 관련

=전수조사경격, 라운드로빈법 이잡기법

* GPT의견 : 핵심: 키가 있어야 읽을 수 있다
## /암호 알고리즘/ 과 키의 분리
**[변경 가능한 부분]이 [키]**

* 암호화키, 복호화키

대칭키 : 키 = 키 (키 배송문제 있음)

비대칭키 : 개인키|공개키 (수학적 원리)

* 혼용

* [secret key -혼용-  [private key
* |                   |
* |shared key         |
* |                   |
* [session key(http)  [public key
# 암호와 보안 상식
## 비밀 **|암호 알고리즘(공개)|**을 사용하지 말 것 

**숨기는 것에 의한 보안(security by obscurity)이라고 부르고 위험하고 어리석은 것으로 간주**
## 약한 암호는 암호화 하지 않는 것보다 위험하다.
[잘못된 안심]
## 어떤 암호라도 언젠가는 해독된다.
**시간과 암호**를 사용하여 지키고 싶은 [평문]**밸런스(trade-off)가 중요**

해독은 어려운 시스템일수록 [좋은 암호] (공격자를 어렵게)

## 암호는 보안의 아주 작은 부분이다.
사회공학 공격, 가장 약한 링크는 암호가 아니라 [사람], (사회공학 공격의 대상은 [사람])

# 암호기법의 분류
## 치환 암호와 전치 암호
**가) 치환 암호(**대치** 암호, Substitution Cipher)**

[대체]한다

의미 : {평문에서 사용하는 문자의 집합} ≠ {암호문에서 사용하는 문자의 집합}

**교환하는 규칙**, **일대일 대응이 아니어도 상관없다** -> a) 단일 문자 암호 : 일대일, b) 다중 문자 암호 : 일대다

**나) 전치 암호(Transpositon Cipher) = 순열(Permutation)**





[재배열]한다
{평문에서 사용하는 문자의 집합} = {암호문에서 사용하는 문자의 집합}

* GPT의견 : 치환 = 값 바꿈 / 전치 = 위치 바꿈
**자리를 바꾸는 규칙**, [일대일 대응]규칙
## 블록 암호와 스트림 암호 *3
* **가) 블록 암호(Block cipher)**
* **8비트(ASCII)** -> DES : 64비트, 16비트(Unicode)

* [Round]를 사용

* **나) 스트림 암호(Stream cipher)**

* [XOR]하여 생성

블록 암호는[블록 단위]로 처리가완료 되므로 어디까지 암호화가 진행되었는가 하는 **내부 상태(메모리값)** 를 가질 필요 없다.

스트림 암호는 데이터의 흐름을 [순차적]으로 처리해가기 때문에 **내부 상태**를 가지고 있다.

이동 통신 등의 무선 데이터 보호에 적합

ex)

WEP - WPA - WPA2

└─────┘

RC4(스트림) AES(블록)

## 스트림 암호 블록 암호
블록 암호는 [높은 확산]



* GPT의견 : 블록 = 한꺼번에 / 스트림 = 순차적

스트림 암호는 **낮은 확산**
## 위치에 따른 암호화의 구분 -> VPN
**가) 링크 암호화(Link Encryption**

1. [홉(hop)]
2. [라우터]는 해더 부분을 해독하고 다시 암호화하고 [진행 방향]으로 보내야 한다

**나) 종단간 암호화(End-to-End Encryption)**

1. 종단간 암호화 [홉] 해독하고 암호화할 필요가 없다
2. 애플리케이션 계층(L7)

## 링크암호화 와 종단간 암호화
링크 암호화 : User-transparent(투명) 운영

종단간 암호화 : [사용자 인증]



* GPT의견 : 링크 = 투명, 종단간 = 안전성↑

## 하드웨어와 소프트웨어 암호시스템
**가) 하드웨어 암호시스템**

전용 하드웨어로 암호화(AES사용)

**나) 소프트웨어 암호시스템**

소프트웨어를 사용한 데이터 암호화


* GPT의견 :

하드웨어 → 전용 장치(AES 칩 등)로 빠르고 안전하게 암호화

소프트웨어 → 프로그램으로 처리, 유연하지만 상대적으로 느림
