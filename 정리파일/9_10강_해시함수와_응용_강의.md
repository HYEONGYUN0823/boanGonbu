# 일방향 해시함수
## 일방향 해시함수의 개요
### 기본 개념
1. 정의역(입력)을 D, 치역(출력)을 R이라 할 때 해시함수 h : D -> R(|D|> |R|)는 [다대일] 대응 함수이다. 이것은 충돌이 반드시 [존재함]을 의미한다
### 일방향 해시함수의 특징 *5
1. [임의] 길이의 메시지로부터 [고정] 길이의 해시값을 계산한다.
2. 해시값을 [고속]으로 계산할 수 있다.
3. [일방향성]을 갖는다.
* [역산]할 수 없다.
4. 메시지가 다르면 해시값도 다르다.
* 무결성을 확인하기 위해 사용한다. 메시지가 [1비트]라도 변화하면 해시값은 [매우 높은] 확률로 다른 값이 되어야 하기 때문이다
* 2개의 다른 메시지가 같은 해시값을 갖는 것을 [충돌](collision)이라고 한다. 일방향 해시함수를 무결성 확인에 사용하기 위해서는 충돌이 발견되어서는 안된다. (충돌은 필연적(다대일)), (제한된 시간내에 충돌되는것을 찾지못하도록 설계)
* 충돌을 발견하는 것이 어려운 성질을 가리켜 충돌 내성(collision resistance)이라고 부른다.

### 해시함수 관련 용어 *5
* 일방향 해시 함수 ≒ 메시지 다이제스트 함수 ≒ 메시지 요약함수 ≒ 암호학적 [해시] 함수
* 입력되는 메시지 ≒ 프리(선) 이미지 (pre-image)
* 출력되는 해시값 ≒ 메시지 다이제스트 ≒ 핑거프린트
* 무결성 ≒ 완전성 ≒ 보전성

# 해시함수의 분류와 보안 요구사항
## 해시함수의 보안 요구 사항 *5
### 개요
1. 암호학적 해시함수는 프리이미지 저항성, 제2프리이미지 저항성, 충돌 저항성 의 3가지 기준을 충족해야 한다.
### 프리이미지 저항성(역상 저항성)
1. 해시값 h = H(x)에 대하여, x는 h의 [선] 이미지(preimage)(입력)라 한다.
2. 프리이미지 저항성이란 주어진 해시함수 [h와 y = h(M)에 대해서] Eve가 y =h(M)를 만족하는 메시지 M(입력값)를 찾아낸다는 것이 매우 힘들어야 한다는 성질이다.
### 제2프리이미지 저항성(두 번째 역상 저항성, [약한] 충돌 내성)
1. 메시지를 [쉽게 위조]할 수 없도록 하는 성질이다.
2. Eve는 메시지 M과 다이제스트 h(M)을 가로챈다. Eve는 h(M) = h(M)를 만족하는 다른 메시지 (M')를 생성한다. Eve는 M'와 h(M')를 Bob에게 보낸다. Eve는 이렇게 해서 메시지를 위조할 수 있는 것이다.
3. 이런 충돌이 발생할 확률은 충돌저항성(강한 충돌 내성) 보다 상대적으로 [낮기] 때문에 [약한] 충돌 내성이라 한다.

### 충돌 저항성(충돌 회피성, 강한 충돌 내성)
1. Eve로 하여금 동일한 다이제스트를 가지는 2개의 메시지를 구하지 못하도록 하는 것이다. 여기서 공격자는 어떤 정보도 없는 상태에서 동일한 다이제스트를 갖는 두 개의 메시지를 생성할 수 있다.
2. 충돌이 발생할 확률은 약한 충돌 내성의 경우보다 상대적으로 높기 때문에 [강한] 충돌 내성이라 한다.

### 해시함수가 가져야 할 성질 *5
* 역상 저항상 : 주어진 임의의 출력 값 y에 대해 y=h(x)를 만족하는 [입력값 x]를 찾는 것이 계산적으로 불가능 하다
* 두 번째 역상 저항성 : 주어진 입력값 x에 대해 h(x)=h(x'), x≠x'을 만족하는 다른 입력값 [x']을 찾는 것이 계산적으로 불가능하다.
* 충돌 저항성 : h(x)=h(x')을 만족하는 임의의 두 입력값 [x, x']을 찾는 것이 계산적으로 불가능하다.

## [전자서명]에 이용되는 해시 함수의 특성 *3
1. 해시값을 고속으로 계산할 수 있다.
2. 약 일방향성(Weak onwayness)
* 해시값 H로부터 h(M) = H가 되는 서명문 [M]을 찾는 것은 계산상 불가능해야 한다.
3. 강 일방향성(Strong onewayness)
* 어떤 서명문 M과 그 해시값 H=h(M)이 주어졌을 때 h(M')=H가 되는 서명문 [M'] ≠ M을 찾는 것이 계산상 불가능해야 한다.
4. 충돌 회피성(collision freeness)
* h(M)=h(M')가 되는 서명문 쌍(M, M')(M≠M')을 차즌 것이 계산상 불가능해야 한다.

→ 여기서 1.의 특성은 해시함수의 성능조건이고, 2, 3, 4의 특성은 해시 함수의 안전성에 관한 제약이다. 2, 3 특성은 해시 함수의 [역함수](일방향성) 를 계산하는 것을 방지하는 기능을 말하며, 4 특성은 서명자가 서명문 [M]에 서명하여 전송한 후 나중에 [M']에 서명하여 전송하였다고 주장하는 이른바 [내부부정]을 방지하기 위한 기능이다.

## 해시함수의 성질들 사이 관계
1. 해시함수의 성질들 사이에는 다음과 같은 곤계가 성립한다.
* **충돌 저항성은 두 번째 역상 저항성을 [보장]한다.**
* 두 번째 역상 저항이 역상 저항을 보장하지 못하며, 역상 저항이 두 번째 역상 저항을 보장하지 못한다.
* 충돌 저항성은 역삭 저항성을 보장하지 않는다.

## 키가 없는 해시 함수와 키(대칭키 -> 인증)를 사용하는 해시함수
### 반복 하시함수
#### 기본개념
1. 입력의 길이가 다양한 해시함수 대신에 고정 길이의 입력을 필요로 하는 [함수]를 만들고 필요한 만큼 반복해서 사용하는 것이다.
#### Merkle-Damgard의 구조
1. Merkle-Damgard의 구조는 [압축함수]가 충돌 내성을 갖게 되는 [반복 해시함수]이다.

# 키가 없는 해시함수
## 기본 개념
1. 해시함수는 내부 압축 함수들로 구성된 연산의 설질에 의해 다음과 같이 세 부분으로 나눌 수 있다.
* 블록 암호를 기초로 한 해시함수
* *전용 해시함수([처음부터] 새로 만드는 것)
* 모듈 연산을 기초로 한 해시함수
## 전용 해시함수 *5
1. 오늘날 사용되고 있ㄴ는 전용 해시함수인 SHA-1, RIPEMD, PRIEMD-128, RIPEMD-160, HAVAL 등은 모두 [MD4]를 기초로 디자인 했다
2. 메시지 다이제스트 (Message Digest)(MD2 -> MD4 -> MD5)
* 최종 버전인 [MD5]는 메시지를 [512비트]로 된 블록들로 나누고 [128비트] 다이제스를 출력한다. 현재 128비트 메시지 다이제스트는 충돌 공격에 내성을 갖기에는 길이가 너무 짧다.
3. SHA(secure Hash Algorithm)
* 해시함수 SHA는 DSS(서명전용)에 사용하기 위해 NSA가 설계하였고, **NIST**에 의해서 배포 됌
* SHA-256, SHA-384와 SHA-512를 SHA-2라고 부른다
* [224]-비트 버전이 추가 되었음
* NIST는 SHA-1을 대체하는 차세대 일방향 해시함수 SHA-3을 제정하기로 함. SHA-3(Keccak)은 AES와 같은 방식

## SHA 해시 알고리즘 비교 *5
<table>
  <tr>
    <td rowspan="2">구분</td>
    <td rowspan="2">SHA-1</td>
    <td colspan="4">SHA-2</td>
  </tr>
  <tr>
    <td>SHA-224</td>
    <td>SHA-256</td>
    <td>SHA-384</td>
    <td>SHA-512</td>
  </tr>
  <tr>
    <td>MD 길이</td>
    <td>160</td>
    <td>224</td>
    <td>256</td>
    <td>384</td>
    <td>512</td>
  </tr>
  <tr>
    <td>최대 메시지 길이</td>
    <td>2^64-1</td>
    <td>2^64-1</td>
    <td>2^64-1</td>
    <td>2^128-1</td>
    <td>2^128-1</td>
  </tr>
  <tr>
    <td>블록 길이</td>
    <td>512</td>
    <td>512</td>
    <td>512</td>
    <td>1024</td>
    <td>1024</td>
  </tr>
  <tr>
    <td>워드 길이</td>
    <td>32</td>
    <td>32</td>
    <td>32</td>
    <td>64</td>
    <td>64</td>
  </tr>
  <tr>
    <td>80</td>
    <td>64</td>
    <td>64</td>
    <td>64</td>
    <td>80</td>
    <td>80</td>
  </tr>
</table>

* **LSH (국산 해시함수)**
* 국가보안기술연구소에서 2014년에 개발 된 경량 해시함수
* LSH-224/256/384/512등으로 구성됌

 *LEA : 블록암호(국산)*

4. RIPEMD-160
* [160비트]
* [유럽]
* 비트코인에서 사용되고 있음
5. Tiger
6. HAVAL

### TIP
HAS160 (국산)

* KCDSA에서 사용할 목적으로 개발

##주요 해시 알고리즘 비교 *3
| 항목 | [MD5] | **SHA-1** | RIPEMD-[160] |
|---|---|---|---|
| 다이제스트 길이 | **128비트** | **160비트** | 160비트 |
| 처리 단위 | 512비트 | 512비트 | 512비트 |
| 단계수 | 64(16번의 4라운드) | 80(20번의 4라운드) | 160(16번의 5병행 라운드) |
| 최대 메시지 크기 | [무한](무한도 있음) | **2^64-1 비트** | 2^64-1 비트 |
| 앤디언 | Little-endian | Big-endian | Little-endian |

## 블록암호 기반 해시함수
1. 대칭키 [블록암호]
## 모듈 연산에 기반을 둔 해시함수
1. 모듈 연산에 기반을 둔 해시함수는 압축 함수의 기반을 모듈 연산의 [반복적인] 수행에 두고 있는 해시함수를 말한다.
2. 속도가 빠르지않음

# 암호학적 해시함수의 응용 *3
## 메시지 인증코드 : 해시함수 + 대칭키
1. 메시지 인증은 [키]가 있는 해시함수로 알려진 메시지 인증 코드 (MAC, Message Authentication Code)를 사용하여 얻어진다

# 랜덤 오라클 모델과 해시함수에 대한 공격
## 랜덤 오가클 모델
### 개요
1. 해싷마수에 대한 이상적인 [수학적]모델이다.
* 임의의 길이를 갖는 메시지에 오라클은 0과 1로 이루어진 난수 스트링인 고정된 길이의 메시지 다이제스트를 생성해서 제공한다

### 비둘기집 원리 (충돌은 필연적(다대일))
1. 이 원리는 만약에 n+1마리의 비둘기가 [n개]의 비둘기집에 들어가 있다면 적어도 한 비둘기집에는 두마리의 비둘기가 들어있다는 뜻
### 생일 문제(생일 공격)
1. 같은 해시값을 생성하는 [2개](쌍)의 메시지를 구하는것
2. 일방향 해시함수의 **강한 충돌 내성**을 꺠고자 하는 공격이다
3. 생일 패러독스
* 2분의1 (0.5)
* N = 23
### 랜덤 오라클 모델에 대한 공격
| 구분 | 공격 난이도 |
|---|---|
| 프리이미지 저항성 | $2^n$ |
| 2차 프리이미지 저항성 | $2^n$ |
| 충돌 저항성 | $2^{n/2}$ |
2. 충돌 공격을 하는것보다 프리이미지 공격이나 제2프리이미지 공격을 하는 것이 훨씬 [어렵다]

## 일방향 해시함수에 대한 공격
### 무차별 공격
1. *3 일방향 해시함수의 [약한] 충돌 내성을 깨고자 하는 공격이다. SHA-1의 경우 해시값이 160비트이므로 $2^{160}$회를 시행하면 원하는 메시지가 발견될 것이라고 기대할 수 있다.
* 강한 충돌 개성 - 생일공격 ($2^{80}$)
* 약한 충돌 내성 - 무차별공격 ($2^{160}$)
### 기타 해시함수 공격의 종류 및 특성
1. 일치블록 [연쇄]공격
* 새로운 메시지 M'를 사전에 다양하게 만들어 놓았다가 공격하고자 하는 메시지 M의 해시함수값 H(M)과 같은 해시함수값을 갖는 것을 골라 사용하는 공격이다.
2. 중간자 [연쇄]공격
* 전체 해시값이 아니라 해시 [중간]의 결과에 대한 [충돌쌍]을 찾는다. 특정 [포인트]를 공격대상으로 한다
3. 고정점 [연쇄]공격
* 메시지 블록과 연쇄변수 쌍을 얻게 되면 연쇄변수가 발생하는 [특정한] 점에서 임의의 수의 동등한 블록들을 메시지의 [중간]에 삽입해도 전체 해시값이 변하지 않는다.
4. 차본 [연쇄]공격
* 해시함수의 공격 : 압축함수의 입출력 [차이]를 조사하여 0의 충돌쌍을 주로 찾아내는 방법을 사용한다.
## 일방향 해시함수로 해결할 수 없는 문제
1. 조작과 변경을 검출할 수 있지만 거짓행세(인증)를 검출하지는 못한다
2. 인증을 수행하기 위한 기술이 메시지 인증코드(무결성+인증)와 전자서명(무결성+인증+부인방지)이다.
# 암호학적 해시함수의 예
## SHA-512
### 기본개념 *3
1. SHA-[512]는 다중-블록 메시지로부터 [512]비트 다이제스트를 생성한다. 각 블록은 [1024비트] 길이를 가짅다.
### 메시지 준비
1. SHA-512에서 요구하는 **메시지의 길이는 $2^{128}$비트를 넘지 않는 것**
### 길이 필드와 패딩 *3
1. 메시지 다이제스트를 생성하기 전에 SHA-512에서는 메시지에 추가적으로 덧붙이는 128비트의 부호 없는 정수 [길이] 필드가 필요하다.
2. 이 필드에는 메시지의 길이가 비트수로 표현된 값이 저장된다. 이 길이는 패딩을 하기 [전]의 원래 메시지 길이를 나타낸다.
3. 메시지 길이 필드는 공격자가 해시값이 같으면서, 입력값이 다른 메시지를 찾는 것을 어렵게 하는 [보안 요소]이다.
### SHA-512의 안전성
1. 현재까지 SHA-512에 숨어있는 약점을 지적한 연구 보고서는 없으며, 동일한 메시지 다이제스트를 갖는 두 메시지(생일공격)를 찾아야 하는 난이도는 $2^{256}$번(다른공격 : $2^{256}$)의 연산을 수행해야 얻을 수 있는 수준이다.
2. SHA-512도 이전에 SHA들과 동일한 구조(MD구조)를 가지고 있고, 수학적으로도 동일하게 작동하기 때문에 우려하고 있다
3. NIST는 2007년에 차세대 해시함수로 사용할 SHA-[3]을 공모하게 되었다
## SHA-3
### 개요
1. SHA-1을 대신하여 새로운 표준이 된 일방향 해시 알고리즘
2. 2012년 KECCAK이라는 알고리즘이 SHA-3으로 결정됨
### 선정 과정 (NIST주관)
1.경쟁방식에 의한 표준화가 진행된 것이다.
### SHA-3 최종 후보와 [SHA-3]의 결정
1. Joan Daemen은 대칭 암호 알고리즘 [AES]의 설계자 이기도 하다.
# 메시지 인증코드(MAC)
## 기본개념 *5
1. 메시지 인증코드란 [무결성]을 확인하고 메시지에 대한 [인증]을 하는 기술 (MAC을 사용하면 [변경]과 [거짓행세] 검출 가능)
2. 송신자 및 수신자가 공유하는 키(대칭키)라는 2개의 입력을 기초로 해서 고정 비트길이의 [출력]을 계산하는 함수이다.
3. 전자서명보다 훨씬 빠르다는것이다.(대칭키 사용하기때문)
## 변경 감지 코드(MDC)
1. [무결성]
## 메시지 인증 코드(MAC)
1. 데이터 출원지 인증을 보장하기 위해 변경 감지 코드를 메시지 인증코드(MAC)로 바꿀 필요가 있다.
2. MDC와 MAC의 차이를 살펴보면 MAC에는 Alice와 Bob 사이에 비밀값이 포함된다는 것이다. 예를 들어 Eve는 가지고 있지 않은 [비밀키]가 두 사람 사이의 [비밀값]이 될 수 있다.

* TIP 상호 비밀키를 알고 두 코드가 동일한 경우
1. 수신자는 메시지가 변경되지 [않았다]->무결성
2. 수신자는 메시지가 [송신자]라고 주장하는 근원지로부터 송신되었음을 확신할 수 있다. 왜냐하면 다른 사람은 [비밀키]를 모르기 떄문이고 어느 누구도 정확한 코드를 갖는 메시지를 만들어낼 수 없기 때문이다._>사용자 인증도 됌

## MAC의 키 배송 문제 (대칭키를 사용해서)
1. MAC에서는 송신자와 수신자가 키를 [공유]할 필요가 있다
